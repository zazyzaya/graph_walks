# Graph Walks

A module to easily generate random walks through a graph, with temporal bias if desired. Quickly get CTDNE or others up and running! 

Also includes original code from torch_cluster.rw to generate random walks that return the indices of the edges traversed. 